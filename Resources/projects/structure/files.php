<p>New projects contain code files <?= $localPlatform ? "(<span class='public-file-name'>.py</span> or <span class='public-file-name'>.cs</span>), notebook files (<span class='public-file-name'>.ipynb</span>), and some configuration files (<span class='public-file-name'>.json</span>)" : "(<span class='public-file-name'>.py</span> or <span class='public-file-name'>.cs</span>) and notebook files (<span class='public-file-name'>.ipynb</span>)" ?>. Run backtests with code files and launch the Research Environment with notebook files. <?=$localPlatform ? "In QuantConnect Cloud Platform, code" : "Code"?> files can contain up to 64,000 characters and notebook files can be up to 128KB in size. <? if ($localPlatform) { ?>In your local workspace, the code files and notebook files can be any size. <? } ?>To keep files small, files can import code from other code files. To aid navigation, you can <a href='<?=$renameFilesLink?>'>rename, move, and delete files</a> in <?=$localPlatform ? "VS Code" : "the web IDE"?>. Notebook files save the input cells, but not the output cells.</p>

