<p><? if ($localPlatform) { ?>Local Platform provides multiple <a href='/docs/v2/local-platform/key-concepts/deployment-targets'>deployment targets</a> to enable you to run backtests on-premise and in the cloud. <? } ?>To run a backtest, <a href="/docs/v2/cloud-platform/projects/getting-started#02-View-All-Projects">open a project</a> and then click the <?php if ($localPlatform) {?><img class='inline-icon' src='https://cdn.quantconnect.com/i/tu/local-platform-backtest-icon.png'> / <?php } ?><img class='inline-icon' src='https://cdn.quantconnect.com/i/tu/cloud-platform-backtest-icon.png'> <span class='icon-name'>Backtest</span> icon. If the project successfully builds, "Received backtest <span class='placeholder-text'>backtestName</span> request" displays. If the backtest successfully launches, the IDE displays the <a href='/docs/v2/cloud-platform/backtesting/results'>backtest results page</a> in a new tab. If the backtest fails to launch due to coding errors, the new tab displays the error. 
<? if ($localPlatform) { ?>
As the backtest executes, you can close Local Platform and Docker Desktop without interfering with the backtest. Just don't quit Docker Desktop.
<? } else { ?>
As the backtest executes, you can refresh or close the IDE without interfering with the backtest because it runs on our cloud servers. The backtest runs up to the start of the <a href='/docs/v2/cloud-platform/backtesting/research-guide#06-Out-of-Sample-Period'>out-of-sample hold out period</a>.
<? } ?></p>
