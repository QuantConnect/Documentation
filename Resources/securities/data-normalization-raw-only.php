<p>The data normalization mode doesn't affect <? if (!$research) {?>the data that LEAN passes to <code class="csharp">OnData</code><code class="python">on_data</code> or the <? } ?>data from <? if (!$research) {?><a href='/docs/v2/writing-algorithms/historical-data/history-requests'><? } ?>history request<? if (!$research) {?></a><? } ?><?=$specificAsset?>.<?=$additionalInformation?> If you change the data normalization mode, it won't change the outcome.</p>
