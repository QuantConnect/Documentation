<p>If you use <code class="csharp">Adjusted</code><code class="python">ADJUSTED</code>, <code class="csharp">SplitAdjusted</code><code class="python">SPLIT_ADJUSTED</code>, or <code class="csharp">TotalReturn</code><code class="python">TOTAL_RETURN</code>, we use the entire split and dividend history to adjust historical prices. This process ensures you get the same adjusted prices, regardless of the 
<? if($writingAlgorithms) { ?>
    backtest end date. The <code class="csharp">ScaledRaw</code><code class="python">SCALED_RAW</code> data normalization is only for <a href='/docs/v2/writing-algorithms/historical-data/history-requests'>history requests</a>. When you use <code class="csharp">ScaledRaw</code><code class="python">SCALED_RAW</code>, we use the split and dividend history before the algorithm's current time to adjust historical prices. The <code class="csharp">ScaledRaw</code><code class="python">SCALED_RAW</code> data normalization model enables you to warm up indicators with adjusted data when you subscribe to <code class="csharp">Raw</code><code class="python">RAW</code> security data.</p>
<? } else { ?> 
    <code>QuantBook</code> time. If you use <code class="csharp">ScaledRaw</code><code class="python">SCALED_RAW</code>, we use the split and dividend history before the <code>QuantBook</code>'s <code>EndDate</code> to adjust historical prices.</p>
<? } ?>