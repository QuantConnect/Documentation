<p>The following <code>Market</code> enumeration members are available for Future Options:</p>

<div data-tree='' data-fields='CBOT,COMEX,NYSELIFFE'></div>

<p>You don't need to pass a <code>Market</code> argument to the <code>AddOptionContract</code> method because the contract <code>Symbol</code> already contains the market.</p>
